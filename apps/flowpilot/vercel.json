{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @flowcore/engine build && pnpm --filter flowpilot build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
