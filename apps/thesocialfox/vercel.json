{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @flowcore/engine build && pnpm --filter socialfox-adapter build && pnpm --filter thesocialfox build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
