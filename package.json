{
  "name": "flowcore-project",
  "version": "0.1.0",
  "private": true,
  "description": "FlowCore — AI Workflow Engine with Trust & Observability",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:flowpilot": "pnpm --filter flowpilot dev",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
